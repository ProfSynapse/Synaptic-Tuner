# Modal coordinator consumer composition proof

The provider-free consumer proof composes the reviewed operational preflight,
generic coordinator and Foundation, Modal retention delegate, host transport,
lazy registration whose six advertised capability flags are false, and
consumer-owned stores and authorities. The
Foundation remains the only mutation authority. One fresh start durably stages
the exact three reserved files and spawns the one-argument worker exactly once;
a fresh service facade over the same durable stores reads the accepted run and
does not repeat either effect.

All provider surfaces are fakes with explicit clients. The test performs no
deployment, cloud call, credential read, or public registration. The lazy
registration continues to advertise all six read, lifecycle,
artifact-streaming, and cost-quote flags as false. Those flags are not a master
start switch: the proof's exact grants and authenticated Foundation lineage are
what authorize its synthetic stage and submit effects. No live path or
advertised read surface is qualified; public cutover remains lead-owned.
